📦 1. code-scanner
Stack: Python + TruffleHog / Semgrep
Purpose: Detect secrets, credentials, PII, and other sensitive content in code repositories

User Inputs Required:

Repository URL or path (GitHub, GitLab, Bitbucket, local path)

Branch name (default: main/master)

Authentication token (if private repo)

Scan type: Secrets / PII / Both

Ignore file paths or patterns (optional)

Custom rules YAML (optional Semgrep config)

🗃 2. blob-scanner
Stack: Python + Presidio + OCR
Purpose: Scan PDF, DOCX, TXT for PII

User Inputs Required:

Blob storage location:

Azure Blob / AWS S3 URL

OR local folder path

File types to scan: pdf, docx, txt, xlsx, etc.

Language for OCR (e.g., en, nl, de)

PII types to detect (default: all supported by Presidio)

Include subfolders (Yes/No)

🖼 3. image-scanner
Stack: Azure Vision API
Purpose: Scan images for facial or visual identifiers

User Inputs Required:

Image source:

Azure Blob / AWS S3 / local path

Detection target:

Faces / License Plates / Text in image / All

Region or sensitivity level (e.g., strict for GDPR high-sensitivity faces)

🧮 4. db-scanner
Stack: Azure Data Factory + Presidio
Purpose: Scan structured databases for PII

User Inputs Required:

Database connection string:

Server, Port, Username, Password

Database type: SQL Server / PostgreSQL / MySQL / Cosmos DB / etc.

Database name

Tables to scan (comma-separated or all)

Columns to exclude (optional)

Presidio PII types to match

Scan sample size (e.g., 1000 rows per table)

🛠 5. api-scanner
Stack: FastAPI + Swagger + NLP
Purpose: Scan API request/response schemas for PII

User Inputs Required:

OpenAPI/Swagger file or live API endpoint URL

Authentication token (for protected APIs)

Scan mode: Static (spec-based) / Live Testing

PII terms or patterns to check (optional custom list)

Include headers in scan (Yes/No)

📤 6. manual-upload-tool
Stack: Streamlit
Purpose: Allow manual upload of files for ad-hoc scanning

User Inputs Required:

File upload: PDF, DOCX, XLSX, CSV, image, etc.

Scan type selection: Text, Table, Image, NLP

Language selection (for OCR/NLP accuracy)

Retention policy: Store temporarily / delete after scan

🌱 7. sustainability
Stack: Azure APIs
Purpose: ESG compliance check for cloud infrastructure

User Inputs Required:

Azure Subscription ID

Tenant ID + Client ID + Secret

Select ESG focus area:

Carbon usage

VM energy score

Storage energy impact

Report format: CSV / PDF / Dashboard

🤖 8. ai-model-scanner
Stack: Azure AI Content Moderator / NLP APIs
Purpose: Scan AI/ML models and dataflows for sensitive content leakage

User Inputs Required:

Model input/output samples (JSON, CSV)

Model documentation or data dictionary (optional)

Leakage type to detect:

PII in training data

Bias indicators

Regulatory non-compliance

Language or context (e.g., health, finance, HR)

🔐 9. soc2-scanner
Stack: Python + Rule Engine
Purpose: Check SOC2-related controls (e.g., logging, access policies)

User Inputs Required:

System logs or log file paths (JSON, log, CSV)

Access control config file (YAML/JSON)

Target checks:

Logging compliance

IAM policy structure

Session timeout rules

Custom SOC2 ruleset (optional)

Scan timeframe (e.g., last 30 days)